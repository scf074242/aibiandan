# 每周编排 Agent 体验巡检问题清单 - 2026-06-24

## 巡检范围

- 真实前台路径：`C:\Users\Administrator\Documents\Playground\aibiandan`
- 启动方式：`npm run dev:agent`
- 访问页面：`http://127.0.0.1:5173/`
- Agent Server：`http://127.0.0.1:3000`
- 浏览器方式：Playwright 复用系统 Edge，操作真实 Vite 前台页面
- 本次限制：完成 3 条业务路径；发起 3 个自然语言编排请求；打开/创建/切换 3 个播放单上下文；查看 3 处代码/运行日志；未继续扩展到真实确认写入

## 自由体验记录

我扮演真实编排员，先从空工作区创建东方卫视电视播单，用口语请求“帮我在9点插入一期看东方，最好是今天适合早间档的那期”，期望看到候选节目、原子命令预览或明确阻断。随后切到轮播单，要求“开一个今天的城市宣传轮播，优先上海文旅和地标短片，先给我推荐候选，别直接写正式播单”，观察候选检索、候选解释和草案/正式播单边界。最后回到电视播单，用“把9点那条潮童天下先删掉，我要看一下会不会动正式播单”验证高风险删除是否进入确认或取消流程。

关键观察：真实前台和 Agent Server 能跑通，但自然语言主链路不稳定。电视插入 26.29s 后失败，轮播推荐 64.66s 才返回散文式建议，删除 18.50s 后失败；期间多处按钮禁用。切换轮播/电视播单后，旧播放单消息和依据仍混入当前工作区。轮播请求虽返回若干《人文上海》推荐，但没有结构化候选选择、没有原子命令预览，也没有把“别直接写正式播单”转成明确的待确认安全语义。

未覆盖区域：未确认任何 pending 写入；未实际执行候选选择；未上传版面草案；未验证 OpenClaw 外部入口。原因是 3 条前台主路径已经暴露阻断或高优问题，按巡检上限停止扩展。

## Bug

| 分类 | 标题 | 发现方式 | 现状/痛点 | Bug 复现路径 | 实际结果 | 期望结果 | 严重程度 | 是否稳定复现 | 初步怀疑原因 | 真实业务场景 | 建议设计或修复方向 | 技术评估 | 影响范围 | 优先级 | 复杂度 | 风险/依赖 | 验收标准 | 建议下一步 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Bug | 真实口语插入无法进入候选或原子预览 | 自由体验 | 电视播单已创建，用户给出时间和节目线索后，系统长时间等待并返回“模型没有正常理解”，未给候选、追问或原子命令预览 | `npm run dev:agent` -> 新建电视播单 -> 输入“帮我在9点插入一期看东方，最好是今天适合早间档的那期” | 26.29s 后失败；左侧播单未改动；详情只显示可执行性和能力边界，未说明哪个槽位失败 | 应识别时间、节目/栏目和早间档约束；若目标冲突，应返回候选或明确追问；不得用通用失败兜底 | P0 阻断 | 本次稳定复现 1/1 | 服务端日志出现 `LLM request timed out after 12000ms`；前台依赖 LLM 返回，缺少本地窄路由和槽位级失败解释 | 编排员用业务口语插入节目，是前台 Agent 的主路径 | LLM 超时后返回结构化失败原因；对明确时间+节目名的窄场景提供可解释兜底；失败反馈列出缺失槽位/候选问题 | 需改 `ChatPanel.vue` 失败展示、`AgentServerRuntime` 错误 envelope、intent/slot fallback；避免扩大 regex 树，只做确定性安全兜底 | 电视插入、候选检索、原子预览 | P0 | M | 依赖 LLM 超时策略和服务端错误 schema | 同样请求在 15s 内返回候选/追问/阻断之一；详情包含时间、节目线索、候选来源和失败原因 | 先加真实前台回归 case，再修错误 envelope 与前台展示 |
| Bug | 切换播放单后旧工作区消息和依据污染当前上下文 | 自由体验 | 从电视播单切到轮播单、再切回电视播单后，右侧仍显示上一播放单失败/推荐详情，且轮播工作区中可见“播单：电视播单”的依据 | 新建电视播单 -> 发起插入失败 -> 新建轮播单 -> 查看 ChatPanel -> 再新建电视播单 | 当前轮播/电视工作区混入旧消息；业务依据和播放单类型不一致，用户无法判断当前 Agent 正在基于哪个播放单回答 | ChatPanel 默认只显示当前播放单工作区消息；旧工作区历史应折叠或可切换查看，不能混入当前主线程 | P0 阻断 | 本次稳定复现 | `workspaceKey` 过滤未覆盖失败消息、详情展开状态或新建播放单消息；消息 scope 与 active workspace 更新不同步 | 编排员在电视播单和轮播单之间切任务，不能让旧任务依据影响当前决策 | 对所有 message、detail、pending、recoverable failure 加 workspaceKey；切换工作区时关闭旧详情并隐藏旧消息 | 已有 `foregroundWorkspaceState`，但 ChatPanel 仍有漏过滤路径；主要是前台状态治理 | 多播放单切换、pending 过期、审计详情 | P0 | M | 依赖现有历史消息可追溯设计 | 新建轮播单后默认只见轮播消息；旧电视失败可从历史入口打开；详情中的播单类型与当前 workspace 一致 | 补 browser regression：电视失败 -> 轮播请求 -> 不显示电视依据 |
| Bug | 高风险删除口语请求没有进入确认流程 | 自由体验 | 用户明确要求删除 9 点 `潮童天下` 并声明要观察是否动正式播单，系统没有生成待确认面板，也没有目标选择或阻断解释 | 新建电视播单 -> 输入“把9点那条潮童天下先删掉，我要看一下会不会动正式播单” | 18.50s 后返回“模型没有正常理解”；未出现“待确认执行/取消”面板 | 删除属于高风险操作，应识别目标并进入确认；若正式播单为空或目标只存在于草案，应明确阻断并解释草案/正式播单边界 | P0 阻断 | 本次稳定复现 1/1 | 自然语言删除依赖 LLM；正式播单/草案状态不一致时没有业务化阻断路径；高风险确认只在已结构化 pending 后可用 | 编排员删除节目必须有可审计预览和确认，不允许失败成泛化文案 | 增加删除意图的确定性安全 gate：识别时间/节目线索后先解析目标对象，再确认或阻断；目标对象必须显示草案/正式播单归属 | 涉及 intent interpreter、target resolver、pending panel；不应直接执行删除 | 删除节目、批量删除、待确认状态 | P0 | M | 依赖正式播单状态模型统一 | 同样请求出现目标卡：`09:00-09:30 潮童天下`，并显示“确认删除/取消”或“目标仅在版面草案，未改正式播单” | 先补删除口语矩阵和前台 pending UI 用例 |
| Bug | Agent 请求耗时与超时反馈不一致，长时间锁住页面 | 自由体验/定向检查 | 服务端 LLM 日志显示 12s 超时，但前台请求分别在 26.29s、64.66s、18.50s 才结束；等待期间快捷操作大面积禁用，且没有可停止入口 | 发起电视插入、轮播推荐或删除请求 | 页面长期“思考中”，按钮禁用；轮播请求最终 64.66s 返回，但无阶段进度；用户不能取消当前 Agent 请求 | 模型超时、检索、候选推荐应有分阶段进度、前台超时和停止；超过阈值恢复输入并给可重试动作 | P1 高 | 本次稳定复现 3/3 | LLM client 12s timeout 与服务端 runtime 总耗时/前台 loading 生命周期未对齐；`ChatPanel` 只有 foreground orchestration stop，不覆盖普通 submit loading | 值班编排不能被单条失败请求锁住近一分钟 | 给 `/api/agent/submit` 加整体 deadline 和 AbortController；普通 Agent submit 也接入停止按钮；按钮只禁用冲突动作 | 需改 HTTP client、server route、ChatPanel loading/stop 状态 | 所有自然语言请求 | P1 | M | 依赖服务端可取消执行和事件流 | 15s 内必须返回结果或可恢复超时；5s 后显示“停止”；停止后无 pending 残留 | 先加超时/取消契约测试，再接 UI stop |

## 体验优化

| 分类 | 标题 | 发现方式 | 现状/痛点 | 真实业务场景 | 建议设计或修复方向 | 技术评估 | 影响范围 | 优先级 | 复杂度 | 风险/依赖 | 验收标准 | 建议下一步 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 体验优化 | 候选推荐以散文输出，不能直接选择或比较 | 自由体验 | 轮播请求返回《人文上海》第001/002/003/004/005/007期推荐，但只是自然语言列表，没有候选卡、选择按钮、排序依据列或下一步动作 | 编排员需要快速选中素材并进入预览/确认，不应从长段文字里复制节目名 | 将候选推荐渲染为结构化卡片：栏目/期号/专题/时长/评分/播放量/素材状态/推荐理由/选择按钮 | 前台已有 insert recommendation panel，可复用但需让推荐类结果也输出 structured candidates | 轮播素材推荐、电视候选选择 | P1 | M | 依赖 runtime 输出候选 schema | 同样请求展示 3-6 张候选卡；点击候选进入预览或草案待确认 | 定义 `businessCandidateRecommendations` schema |
| 体验优化 | 空轮播单提示把“别写正式播单”误导成必须先有草案 | 自由体验 | 用户要求“先给推荐候选，别直接写正式播单”，系统最后提示“当前还没有可更新的草案”，容易让用户误以为推荐候选必须依赖草案 | 编排员只是想先找素材、看候选，不一定已有草案 | 区分“候选检索/素材推荐”和“草案更新/正式写入”；无草案时仍可给候选卡和“加入草案/创建草案”动作 | 主要是文案和 decision kind 映射 | 轮播工作区、草案边界 | P2 | S | 依赖候选结果结构化 | 推荐请求不再提示必须先有草案；只在用户要求更新草案时提示 | 调整反馈文案和测试断言 |
| 体验优化 | 详情面板对业务用户仍偏内部化 | 定向检查 | 失败详情显示“可执行：8/8、规则：阻断8,提醒7、source_missing”等内部字段，但没有直接解释“为什么没懂/哪里缺证据” | 编排员需要知道下一句该补什么，而不是读内部 readiness | 详情默认显示业务解释：已识别线索、未识别槽位、查了哪些候选源、下一步建议；内部字段折叠到调试区 | 需要 evidence adapter，不改核心执行 | 失败、阻断、候选解释 | P2 | M | 依赖 details schema | 失败详情首屏能回答“缺什么/下一步说什么”；内部 trace 默认隐藏 | 新增 business-facing detail formatter |

## 新功能

| 分类 | 标题 | 发现方式 | 现状/痛点 | 真实业务场景 | 建议设计或修复方向 | 技术评估 | 影响范围 | 优先级 | 复杂度 | 风险/依赖 | 验收标准 | 建议下一步 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 新功能 | 播放单工作区历史抽屉 | 自由体验 | 为避免污染当前上下文，旧消息应隐藏；但审计上又需要可追溯历史 | 编排员切换电视/轮播后，需要专注当前任务，同时偶尔回看旧播单决策 | 增加“历史工作区”抽屉，按播放单和时间分组显示旧消息、候选、pending 结果 | 前端功能，复用 message workspaceKey | 多播放单工作 | P2 | S | 依赖 workspaceKey 完整性 | 当前线程不混旧消息；历史抽屉可查看旧电视/轮播会话 | 先修过滤，再做抽屉 |
| 新功能 | 候选到原子命令预览的一键链路 | 自由体验 | 推荐候选后没有“选择此候选 -> 预览插入/替换/加入草案”的操作 | 编排员需要从素材推荐自然推进到编排动作 | 候选卡提供“预览加入队列/预览插入到当前位置/仅收藏”动作，并生成原子命令预览 | 需连接 candidate recommendation 与 pending command builder | 轮播推荐、电视插入 | P1 | M | 依赖候选 schema 和 pending mutation | 选择候选后出现待确认原子命令，确认前不写正式播单 | 设计 candidate action contract |
| 新功能 | 真实 LLM 失败后的“一键改写并重试” | 定向检查 | 失败只提示“重试或把时间、节目名再说一遍”，不会根据已识别线索生成更好的重试请求 | 编排员遇到模型失败时希望系统帮忙缩小范围 | 失败卡提供“按时间+节目重试”“只查候选”“改为草案操作”等 quick replies | 前台可基于 failure details 生成 quick reply | 失败恢复 | P2 | S | 依赖结构化失败原因 | 点击 quick reply 能发出更具体的重试请求，并保留原始审计 | 先补 failure details |

## 架构变更

| 分类 | 标题 | 发现方式 | 现状/痛点 | 真实业务场景 | 建议设计或修复方向 | 技术评估 | 影响范围 | 优先级 | 复杂度 | 风险/依赖 | 验收标准 | 建议下一步 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 架构变更 | 统一 Agent submit 的超时、取消和错误 envelope | 定向检查 | LLM client 超时、server runtime 总耗时、HTTP response 和 ChatPanel loading 状态各自处理，导致前台等待远超底层超时 | 真实编排中模型或素材库慢是常态，系统必须可控恢复 | 为 `/api/agent/submit` 定义统一 deadline、abort signal、error envelope、recoverable action；前台只消费结构化状态 | 跨 `llmClient`、`AgentServerRuntime`、`agent-server.mjs`、`HttpAgentRuntimeClient`、`ChatPanel` | 所有自然语言 Agent 请求 | P0 | L | 依赖服务端事件流/stop endpoint | 超时、取消、失败均有同一 envelope；前台 15s 内恢复或进入可停止状态 | 先写 API contract 和 3 个失败回归 |
| 架构变更 | 工作区隔离从展示层提升到会话/消息模型 | 自由体验/定向检查 | 现有过滤不足以阻止旧消息、详情和 recoverable failure 穿透到新播放单 | 编排员把播放单当作 active workspace，切换后旧 pending/失败不得影响当前任务 | 在 server session、front message、pending、details、recoverable failure 中统一 `workspaceKey`、`playlistId`、`playlistType`；切换时服务端也标记旧 pending stale | 跨前后端状态模型；已有 `foregroundWorkspaceState` 可作为基础 | 播放单切换、多轮上下文、pending 过期 | P0 | L | 依赖 session store 和 replay 兼容 | 切换播放单后旧 pending 不能确认；旧消息默认隐藏；服务端 replay 按 workspace 分组 | 先补状态模型文档和前台/服务端测试 |
| 架构变更 | 将候选推荐结果纳入正式 business evidence schema | 自由体验 | 轮播候选由 LLM 散文生成，无法被 UI、pending 或审计复用 | 真实候选选择需要结构化字段和可审计排序原因 | LLM 可写解释，但候选必须来自 structured `businessEvidence.candidates`，包含 query facets、match fields、rank reason、candidate id | 需改 runtime 输出和 ChatPanel renderer | 候选检索、选择、原子预览、审计 | P1 | M | 依赖素材库字段质量 | 候选推荐、选择、确认共用同一 candidate id；审计可回放 | 定义 schema 后改轮播推荐链路 |

## 本周优先排期建议

| 排名 | 事项 | 原因 | 建议范围 |
|---|---|---|---|
| 1 | P0：统一 Agent submit 超时/取消/错误 envelope | 三条真实自然语言路径都被慢响应或泛化失败影响，先解决可恢复性 | `llmClient`、`AgentServerRuntime`、`ChatPanel` |
| 2 | P0：工作区隔离提升到消息和服务端会话模型 | 切换电视/轮播后仍混旧依据，会直接造成误操作风险 | `foregroundWorkspaceState`、server session、ChatPanel |
| 3 | P0：删除/插入等高风险口语请求的确认或阻断兜底 | 删除不能依赖模型成功才进入安全门禁；明确目标时必须可审计 | intent slots、target resolver、pending panels |
| 4 | P1：候选推荐结构化并接原子预览 | 轮播推荐已有业务价值，但散文结果不能进入编排闭环 | candidate schema、ChatPanel 候选卡、pending builder |
