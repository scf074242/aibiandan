# 每周编排 Agent 体验巡检问题清单 - 2026-06-23

## 巡检范围

- 真实前台路径：`C:\Users\Administrator\Documents\Playground\aibiandan`
- 启动方式：`npm run dev:agent:persist`
- 访问页面：`http://127.0.0.1:5173/`
- 健康状态：`agent:health` 正常；server runtime、file session persistence、LLM configured 均可用
- 本次限制：最多 3 条端到端业务路径、10 个自然语言请求、3 个播放单/任务上下文、5 次候选/解释检查

## 自由体验记录

我扮演东方卫视/轮播业务编排员，实际走了 3 条路径：

1. 未建播单时直接下达插入请求，验证阻断和新建播单引导。
2. 创建电视播单后，用“9点潮童天下换成今晚这一期，先看候选和原子命令”进行替换、纠偏、取消。
3. 切换到轮播单后，请求“上海旅游宣传短片三分钟左右放开头”，检查候选检索和证据展示。

关键观察：系统能进入真实 `ChatPanel` / `broadcast-plan` 页面，但草案和正式播单边界混乱，预览前发生了草案写入，取消不回滚，长耗时期间缺少停止入口。

未覆盖区域：未实际确认写入候选、未跑全天编排、未验证上传草案文件。

## Bug

| 分类 | 标题 | 发现方式 | 现状/痛点 | Bug 复现路径 | 实际结果 | 期望结果 | 严重程度 | 是否稳定复现 | 初步怀疑原因 | 真实业务场景 | 建议设计或修复方向 | 技术评估 | 影响范围 | 优先级 | 复杂度 | 风险/依赖 | 验收标准 | 建议下一步 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Bug | “先看候选/原子命令”仍直接改草案且改错时段 | 自由体验 | 用户明确要求预览，系统却把左侧版面草案 `07:00-09:00 看东方` 改成 `今晚`，目标本应是 `09:00-09:30 潮童天下` | 新建电视播单 -> 输入“把9点的潮童天下换成今晚这一期，先给我看候选和原子命令” | 回复候选时已更新草案第 2 段；正式播单 itemCount 仍为 0 | 只返回候选、目标匹配解释和原子命令预览，不写草案/正式播单 | P0 阻断 | 本次稳定复现 | ReAct/candidate_precheck 被导向 `layout_draft` 更新；`targetSegmentIndex` 与时间语义映射错位 | 编排员要求先审核替换方案，不能在确认前改播单或草案 | 所有候选预检统一加 `noMutationBeforeConfirm` 写屏障；时间目标优先匹配正式播单/草案同名时段并显示匹配证据 | 需改 runtime 决策和前台写入门禁；相关代码见 `src/services/runtime/demoRuntimeFacade.ts` 约 1393、`src/components/dialogue/ChatPanel.vue` 约 1736 | 电视播单替换、候选预检、草案编辑 | P0 | M | 依赖草案/正式播单状态模型澄清 | 同样请求只出现候选和“待确认”，左侧任何数据不变；目标显示为 `09:00-09:30 潮童天下` | 先加回归测试，再加 runtime 写屏障 |
| Bug | 取消待确认不回滚已误改草案 | 自由体验 | 用户说取消且不要改草案，系统显示“已取消”，但左侧草案仍保留 `07:00-09:00 今晚` | 延续上一步 -> 输入“取消刚才操作，不要改版面草案...” -> 点击取消 | pending 插入被取消，草案未恢复 | 取消应保证本轮未确认改动全部撤销，或明确告知无法回滚并提供恢复按钮 | P0 阻断 | 本次稳定复现 | 草案更新不是 pending transaction，取消只清理 `pendingAtomicContext` | 编排员发现 AI 改错后取消，页面必须回到可信状态 | 草案/正式播单写入统一走可回滚事务；取消动作绑定 mutation id | 需要状态快照、mutation journal 或延迟提交 | 所有确认/取消流程 | P0 | M | 依赖前台草案状态管理 | 取消后草案恢复原始 `看东方`；事件日志显示 rollback/noMutation | 为 layout draft update 增加 pending 草案和 rollback 测试 |
| Bug | “电视播单”工作区显示与服务端正式播单状态矛盾 | 定向检查 | 左侧显示当前工作区为电视播单并列出 19 段，但服务端事件 `formalPlaylistItemCount: 0`，Agent 又说正式播单为空 | 新建电视播单 -> 发起替换请求 -> 纠偏“正式播单里的潮童天下” | 系统把替换降级为“插入推荐”，并说正式播单为空 | UI 应清楚区分“版面草案”与“正式播单”；正式播单为空时不应让用户误以为可替换其中节目 | P0 阻断 | 本次稳定复现 | workspace label、left panel data source、runtime formal snapshot 未对齐 | 编排员打开播单后要知道自己正在改草案、正式播单还是素材队列 | 顶部工作区拆成“正式播单状态”和“参考草案状态”；Agent context 注入同一份可见状态 | 需跨 `broadcast-plan`、ChatPanel、server session 统一数据契约 | 所有电视播单 Agent 操作 | P0 | L | 影响现有草案进入编排流程 | 空正式播单时替换请求被明确阻断或引导“先按草案生成正式播单”；UI 无歧义 | 先定义 playlist/draft state contract |
| Bug | 轮播素材证据包显示层级不适合业务用户 | 定向检查 | 主详情只显示“候选结果：0 个”，原始明细才有 queries，且把整句原文也作为查询 | 新建轮播单 -> 请求上海旅游宣传短片 -> 详情 -> 明细 -> 查看原始明细 | 业务证据不足，原始 JSON 裸露 | 主详情展示拆解后的查询词、过滤条件、排除原因；原始 JSON 仅调试态 | P2 中 | 本次稳定复现 | `ChatPanel.vue` 直接 `<pre>` 渲染 details；查询生成仍追加整句兜底 | 编排员需要判断“没找到”是库里没有还是搜索词不对 | 增加业务化 evidence view；隐藏/下载原始包；避免整句默认查询 | 前端整理为主，检索策略小改；相关代码见 `src/components/dialogue/ChatPanel.vue` 约 207 | 候选检索、拒绝解释 | P2 | S | 依赖 details schema 稳定 | 详情可见 queries、0 候选原因、下一步建议；无裸 JSON 默认展开 | 先改详情组件和快照测试 |

## 体验优化

| 分类 | 标题 | 发现方式 | 现状/痛点 | 真实业务场景 | 建议设计或修复方向 | 技术评估 | 影响范围 | 优先级 | 复杂度 | 风险/依赖 | 验收标准 | 建议下一步 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 体验优化 | 长耗时“思考中”缺少停止/后台运行 | 自由体验 | 未建单阻断约 16s，电视替换约 48s，轮播候选约 56s；期间快捷操作全部禁用 | 编排员高频微调，不能被单次检索锁死近一分钟 | 思考态显示阶段、可停止、可后台继续；只禁用冲突动作，不禁用新建/切换等安全动作 | 前端状态和 server task stop endpoint 已有基础，可接入 | ChatPanel 全部请求 | P1 | M | 要避免停止后残留 pending | 5s 后显示可停止；停止后输入恢复且无 mutation | 先接 `execution/stop` 和 react task stop 到 UI |
| 体验优化 | 跨上下文消息清理不一致 | 自由体验 | 切到轮播单后电视替换消息消失，但最早“请先创建播单”仍残留 | 编排员切任务时需要看到当前上下文相关历史 | 按 workspace 分组消息；切换时显示当前 workspace 会话，历史折叠 | 前端会话过滤 | 播单切换、创建 | P2 | S | 需保留审计可追溯 | 新轮播单只显示轮播相关消息，旧消息可从历史展开 | 加 workspaceKey 到 message scope |
| 体验优化 | 候选推荐缺少“为什么是它”摘要 | 自由体验 | 今晚候选列出期号/主题/评分，但未解释最新一期、主题匹配、重复“第001期”差异 | 编排员选片要看栏目连续性、期号、专题、素材状态 | 候选卡增加期号排序、是否最新、重复版本差异、素材状态 | 数据已有一部分，需规范展示 | 候选选择 | P1 | M | 依赖素材字段可信度 | 候选卡能解释推荐顺序和重复版本 | 先补候选 explain schema |

## 新功能

| 分类 | 标题 | 发现方式 | 现状/痛点 | 真实业务场景 | 建议设计或修复方向 | 技术评估 | 影响范围 | 优先级 | 复杂度 | 风险/依赖 | 验收标准 | 建议下一步 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 新功能 | 草案变更预览与差异确认 | 定向检查 | 草案会被直接更新，用户看不到改前/改后 diff | 用草案微调全天编排结构时，编排员需要审查时段变化 | 增加草案 diff 面板：时段、原栏目、新栏目、原因、确认/撤销 | 需 draft snapshot + diff 组件 | 版面草案编辑 | P1 | M | 依赖草案事务化 | 未确认前只显示 diff；确认后才更新左侧草案 | 与 P0 写屏障一起设计 |
| 新功能 | 正式播单从草案生成的显式入口 | 自由体验 | 创建电视播单后正式播单为空，但左侧主要展示草案 | 编排员需要从版面草案生成正式串联单后再做替换/移动 | 明确按钮“按草案生成正式播单”，生成后左侧切到正式播单 | 已有“按这个开始编排”语言，可产品化为主入口 | 电视播单主流程 | P1 | M | 依赖生成流程质量 | 替换操作前能看到正式播单 itemCount > 0 | 先做最小正式播单生成状态 |
| 新功能 | 素材检索重试/扩展查询工作流 | 定向检查 | 0 候选时只给自然语言建议，不能一键重试 | 编排员会换关键词、放宽时长、换专题继续找 | 0 候选卡提供“放宽时长”“换关键词”“按地标查”等按钮 | 可复用 ReAct task continue | 轮播候选检索 | P2 | M | 需防止无限搜索 | 点击后生成新 queries 并保留旧证据 | 补 continue UI 和 query chips |

## 架构变更

| 分类 | 标题 | 发现方式 | 现状/痛点 | 真实业务场景 | 建议设计或修复方向 | 技术评估 | 影响范围 | 优先级 | 复杂度 | 风险/依赖 | 验收标准 | 建议下一步 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 架构变更 | 统一草案、正式播单、pending mutation 的状态模型 | 定向检查 | `layout_draft` 决策可直接 emit 到前台；formal playlist snapshot 仍为空；pending 只覆盖部分原子操作 | 编排员在一个工作区内连续创建、预览、确认、取消、切换任务 | 建立状态机：draft reference、draft pending mutation、formal playlist、formal pending mutation，所有写入必须有 owner、workspaceKey、mutationId | 跨 runtime、server session、ChatPanel、broadcast-plan | 全 Agent 主链路 | P0 | L | 需要回归覆盖多路径 | 任一请求能判定目标对象；取消/切换可过期或回滚；事件日志一致 | 先写状态契约文档和 P0 回归用例 |
| 架构变更 | ReAct 候选预检与草案更新职责分离 | 自由体验 | candidate_precheck 被用于直接草案更新 | “先查候选”不等于“更新草案方向” | `candidate_precheck` 只能产证据；`draft_update` 必须显式确认或用户明确要求“改草案” | 主要改 planner action schema 和 runtime 分发 | 候选检索、草案微调 | P0 | M | 需兼容旧测试 | “给我候选”永不触发 `layout_draftUpdated` | 拆 action 类型并更新 prompt/tests |
| 架构变更 | 面向业务的 evidence schema，而不是裸 details | 定向检查 | UI 直接格式化内部 details，暴露 planner trace、JSON、内部 owner | 编排员需要业务证据，开发才需要 trace | 分层：businessEvidence、auditTrace、debugRaw；默认只显示 businessEvidence | 中等，需 schema migration | ChatPanel details、审计包 | P2 | M | 依赖现有测试调整 | 默认详情无内部 JSON；调试模式可复制 trace | 先定义 evidence adapter |

## 本周优先排期建议

| 排名 | 事项 | 原因 | 建议负责人/范围 |
|---|---|---|---|
| 1 | P0：草案/正式播单/pending mutation 状态模型统一 | 当前已经出现确认前改草案、取消不回滚、正式播单为空却显示电视播单的组合问题，这是主链路可信度问题 | Runtime、ChatPanel、broadcast-plan |
| 2 | P0：`candidate_precheck` 写屏障 | “先看候选/原子命令”是编排员最常用的安全操作，必须保证无确认不写入 | Runtime planner、pending context、前台门禁 |
| 3 | P1：长耗时任务停止与状态恢复 | 48-56 秒等待会锁住工作区，真实值班编排时不可接受，也会放大误操作后的焦虑成本 | ChatPanel、agent server task stop |

