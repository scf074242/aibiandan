# 下一步开发规划

> **版本**: v1.0  
> **日期**: 2026-03-24  
> **目标**: 基于现有 create.vue 串联单编辑页面，接入 Kimi 2.5 LLM 实现智能编排功能

---

## 一、项目现状分析

### 1.1 当前项目结构
```
bigbiandan2/
├── src/
│   ├── components/          # 基础组件（HelloWorld, TheWelcome等）
│   ├── views/
│   │   ├── HomeView.vue
│   │   └── AboutView.vue
│   ├── router/
│   ├── stores/
│   ├── App.vue
│   └── main.ts
├── create.vue               # 已有的串联单编辑页面（在根目录）
├── package.json             # 已安装 openai, element-plus, dayjs 等依赖
└── ...
```

### 1.2 已有功能（create.vue）
- ✅ 串联单基础编辑功能
- ✅ 节目添加/删除/编辑
- ✅ 版面参考显示
- ✅ 时间轴展示
- ✅ 基础校验（未关联成品、空成品、时间 discontinuity）

### 1.3 待开发功能
- ❌ LLM 智能编排
- ❌ AI 对话微调
- ❌ 自动校验修复
- ❌ 编排进度可视化

---

## 二、开发阶段规划

### 阶段一：项目结构整理与基础配置（第1天）

#### 任务 1.1: 移动 create.vue 到正确位置
**文件操作**:
- 将 `create.vue` 移动到 `src/views/broadcast-plan/create.vue`
- 创建 `src/views/broadcast-plan/` 目录
- 更新路由配置

#### 任务 1.2: 创建核心目录结构
```
src/
├── services/
│   ├── llm/                 # LLM 相关服务
│   │   ├── llmClient.ts     # LLM 客户端封装
│   │   ├── llmConfig.ts     # 配置管理
│   │   ├── promptBuilder.ts # Prompt 构建器
│   │   ├── contextBuilder.ts# 上下文构建
│   │   ├── responseParser.ts# 响应解析器
│   │   └── prompts/         # Prompt 模板
│   │       ├── systemPrompts.ts
│   │       └── templates.ts
│   ├── orchestrator.ts      # 编排调度器
│   └── commandExecutor.ts   # 命令执行器
├── components/
│   ├── llm/
│   │   └── LLMConfigPanel.vue       # LLM 配置面板
│   ├── orchestration/
│   │   └── OrchestrationProgress.vue # 编排进度面板
│   └── dialogue/
│       └── ChatPanel.vue            # AI 对话面板
├── composables/
│   ├── useOrchestrator.ts   # 编排组合式函数
│   └── useLLMChat.ts        # 对话组合式函数
└── types/
    └── llm.ts               # LLM 相关类型定义
```

#### 任务 1.3: 环境变量配置
**文件**: `.env.development`
```bash
# Kimi 2.5 API 配置
VITE_CODE_PLAN_LLM_BASE_URL=https://api.moonshot.cn/v1
VITE_CODE_PLAN_LLM_API_KEY=your_api_key_here
VITE_CODE_PLAN_LLM_MODEL=kimi-k2.5
```

---

### 阶段二：LLM 基础设施（第2天）

#### 任务 2.1: LLM 类型定义
**文件**: `src/types/llm.ts`
```typescript
// 核心类型定义
export interface LLMConfig {
  baseURL: string
  apiKey: string
  model: string
  temperature: number
  maxTokens: number
  timeout: number
}

export interface ChatMessage {
  role: 'system' | 'user' | 'assistant'
  content: string
}

export interface LLMResponse {
  content: string
  usage?: {
    promptTokens: number
    completionTokens: number
    totalTokens: number
  }
}

// Command 类型
export type CommandType = 
  | 'plan'           // 规划模式
  | 'query_candidates' // 查询候选
  | 'fill_item'      // 填充节目
  | 'insert'         // 插入
  | 'delete'         // 删除
  | 'replace'        // 替换
  | 'swap'           // 交换
  | 'move'           // 移动
  | 'update_field'   // 更新字段
  | 'batch'          // 批量操作

export interface ScheduleCommand {
  action: CommandType
  data: any
  reasoning?: string
}
```

#### 任务 2.2: LLM 客户端封装
**文件**: `src/services/llm/llmClient.ts`

**功能**:
- OpenAI SDK 初始化（兼容 Moonshot API）
- 请求/响应拦截
- 指数退避重试机制（最多3次）
- Token 使用量统计
- 错误分类处理

**核心方法**:
```typescript
export class LLMClient {
  constructor(config?: Partial<LLMConfig>)
  async chat(messages: ChatMessage[], options?: ChatOptions): Promise<LLMResponse>
  async chatStream(messages: ChatMessage[], options?: ChatOptions): AsyncGenerator<string>
  getTokenUsage(): TokenUsageStats
  testConnection(): Promise<boolean>
}
```

#### 任务 2.3: LLM 配置管理
**文件**: `src/services/llm/llmConfig.ts`

**功能**:
- 读取环境变量配置
- 本地存储持久化
- 配置验证
- 连接测试

---

### 阶段三：Prompt 工程（第3-4天）

#### 任务 3.1: 系统 Prompt 设计
**文件**: `src/services/llm/prompts/systemPrompts.ts`

**四种角色 Prompt**:

1. **编排专家角色**（Phase 1-3 使用）:
```typescript
export const ORCHESTRATION_EXPERT_PROMPT = `你是广电节目编排专家，负责为电视频道生成每日串联单。

你的职责：
1. 根据频道属性、版面参考、历史数据制定编排计划
2. 将全天拆分为15-20个时段块
3. 为每个时段块选择合适的节目
4. 确保编排符合业务规则和约束

约束条件：
- 黄金时段（19:00-22:00）必须安排高收视率节目
- 广告必须成组出现，每组2-5条
- 直播节目必须匹配可用演播室
- 节目时长必须匹配时段块

输出格式：
你必须输出JSON格式的命令，不要输出任何解释性文字。`
```

2. **对话助手角色**（对话微调使用）:
```typescript
export const DIALOGUE_ASSISTANT_PROMPT = `你是广电编排助手，帮助用户通过自然语言修改串联单。

你的职责：
1. 理解用户的自然语言意图
2. 识别涉及的节目、时间、操作类型
3. 将意图转换为标准化的 ScheduleCommand
4. 在不确定时提出澄清问题

支持的意图类型：
- INSERT: 插入节目
- DELETE: 删除节目
- REPLACE: 替换节目
- SWAP: 交换位置
- MOVE: 移动位置
- UPDATE_FIELD: 更新字段
- SHIFT_TIME: 调整时间

输出格式：
你必须输出JSON格式的命令或澄清问题。`
```

#### 任务 3.2: 上下文构建器
**文件**: `src/services/llm/contextBuilder.ts`

**功能**:
- 版面参考摘要生成
- 历史串联单摘要
- 节目库过滤（按频道+时段）
- 当前串联单摘要
- 空窗列表格式化
- Token 预算管理

**核心方法**:
```typescript
export class ContextBuilder {
  buildPlanningContext(channelId: string, date: string): PlanningContext
  buildFillingContext(gap: GapInfo, scheduleItems: ScheduleItem[]): FillingContext
  buildRepairContext(errors: ValidationError[], items: ScheduleItem[]): RepairContext
  buildDialogueContext(userInput: string, history: ChatMessage[]): DialogueContext
}
```

#### 任务 3.3: Prompt 构建器
**文件**: `src/services/llm/promptBuilder.ts`

**四种 Prompt 模式**:

1. **Phase 1 - 规划模式**:
```typescript
buildPlanningPrompt(context: PlanningContext): ChatMessage[]
// 输入：频道信息、版面参考、历史数据
// 输出：PlanCommand（15-20个TimeBlock）
```

2. **Phase 2 - 渐进式填充（两阶段）**:
```typescript
// 阶段1：查询候选
buildQueryCandidatesPrompt(gap: GapInfo, context: Context): ChatMessage[]
// 输出：QueryCandidatesCommand

// 阶段2：选择填充
buildFillItemPrompt(gap: GapInfo, candidates: ProgramCandidate[], context: Context): ChatMessage[]
// 输出：FillItemCommand
```

3. **Phase 3 - 修补模式**:
```typescript
buildRepairPrompt(errors: ValidationError[], items: ScheduleItem[]): ChatMessage[]
// 输出：修补命令（insert/replace/delete等）
```

4. **对话微调模式**:
```typescript
buildDialoguePrompt(userInput: string, context: DialogueContext): ChatMessage[]
// 输出：ScheduleCommand 或 ClarificationQuestion
```

---

### 阶段四：响应解析与命令执行（第5天）

#### 任务 4.1: 响应解析器
**文件**: `src/services/llm/responseParser.ts`

**功能**:
- 去除代码块包裹（```json）
- JSON 解析错误处理
- action 字段合法性校验
- 字段完整性校验
- 类型转换和默认值填充
- 错误分类和报告

```typescript
export class ResponseParser {
  parse(response: string): ParseResult<ScheduleCommand>
  validateCommand(command: ScheduleCommand): ValidationResult
  handleParseError(error: Error, rawResponse: string): ParseResult<never>
}
```

#### 任务 4.2: 命令执行器
**文件**: `src/services/commandExecutor.ts`

**功能**:
- Command 到原子能力的映射
- 批量命令处理
- 执行结果收集
- 错误回滚机制
- 执行日志记录

**映射关系**:
| Command | 操作 |
|---------|------|
| plan | 设置编排计划 |
| fill_item | 添加节目到串联单 |
| insert | 插入节目 |
| delete | 删除节目 |
| replace | 替换节目 |
| swap | 交换节目位置 |
| move | 移动节目位置 |
| update_field | 更新节目字段 |

---

### 阶段五：编排调度器（第6-7天）

#### 任务 5.1: 编排调度器核心
**文件**: `src/services/orchestrator.ts`

**功能实现**:

**Phase 0 - 初始化**:
- 加载版面参考数据
- 加载历史串联单数据
- 初始化空窗列表
- 验证 LLM 配置

**Phase 1 - 规划**:
- 构建规划 Prompt
- 调用 LLM 获取 PlanCommand
- 验证规划结果
- 渲染编排计划进度面板
- 等待用户确认（可选）

**Phase 2 - 渐进式填充循环**:
```typescript
async function phase2Filling(gaps: GapInfo[]) {
  for (const gap of gaps) {
    // 阶段1：查询候选
    const queryCmd = await llm.generateQueryCandidates(gap, context)
    const candidates = await queryCandidatesByMultiSource(queryCmd.searchCriteria)
    
    // 阶段2：选择填充
    const fillCmd = await llm.generateFillItem(gap, candidates, context)
    const program = await getProgramByCode(fillCmd.selectedProgram.programCode)
    
    // 构建并执行
    const scheduleItem = buildScheduleItem(program, gap)
    await appendScheduleItem(scheduleItem)
    
    // 更新进度
    updateProgress(gap.gapId, 'completed')
    
    // 检查用户中断
    if (isCancelled) break
  }
}
```

**Phase 3 - 修补**:
- 执行最终全量校验
- 若存在错误，生成修补 Prompt
- 调用 LLM 获取修补命令
- 执行修补（最多3轮）
- 报告修补结果

#### 任务 5.2: 编排状态管理
**文件**: `src/composables/useOrchestrator.ts`

**功能**:
- 编排状态管理（idle/planning/filling/repairing/completed/error）
- 进度计算
- 错误收集
- 用户中断处理
- 重试机制

---

### 阶段六：UI 组件开发（第8-10天）

#### 任务 6.1: LLM 配置面板
**文件**: `src/components/llm/LLMConfigPanel.vue`

**功能**:
- API Key 输入（密码框）
- Base URL 输入
- 模型选择下拉框（kimi-k2.5 / kimi-k2.5-32k）
- 温度滑块（0.0-1.0）
- 连接测试按钮
- 配置保存/加载

#### 任务 6.2: 编排进度面板
**文件**: `src/components/orchestration/OrchestrationProgress.vue`

**功能**:
- 时段块列表（TodoList 样式）
- 状态标识（待填充/进行中/完成/错误）
- 当前执行块高亮
- 错误块标记
- 总体进度百分比
- 取消编排按钮
- 实时日志输出

#### 任务 6.3: AI 对话面板
**文件**: `src/components/dialogue/ChatPanel.vue`

**功能**:
- 消息列表（用户/AI）
- 命令预览卡片（显示 AI 生成的命令）
- 确认/取消按钮
- 澄清问题展示
- 快捷指令按钮（常用操作）
- 输入框 + 发送
- 对话历史

---

### 阶段七：与 create.vue 集成（第11天）

#### 任务 7.1: 集成 AI 功能到 create.vue

**修改内容**:
1. **顶部工具栏添加"AI 智能编排"按钮**:
   - 在现有按钮组中添加 AI 编排入口
   - 点击打开编排配置对话框

2. **编排确认对话框**:
   - 显示编排参数配置
   - 确认后开始编排流程

3. **编排进度抽屉**:
   - 右侧滑出显示编排进度
   - 实时更新填充状态

4. **AI 对话 Tab 页**:
   - 在右侧边栏添加对话面板
   - 支持自然语言修改串联单

#### 任务 7.2: 表格异常高亮

**修改文件**: `create.vue` 中的表格组件

**功能**:
- 行级 CSS 类动态绑定（根据校验状态）
- 单元格级异常标记
- Tooltip 错误信息
- 多错误叠加样式

---

### 阶段八：测试与优化（第12-13天）

#### 任务 8.1: 端到端测试

**测试场景**:
- 完整编排流程（新闻综合频道）
- 对话微调流程
- 校验错误自动修复
- 边界情况处理（空数据、网络错误等）

#### 任务 8.2: Prompt 调优

**优化方向**:
- 根据测试结果调整 Prompt
- 优化上下文压缩策略
- 提高命令生成准确率

#### 任务 8.3: 性能优化

**优化方向**:
- LLM 响应缓存
- 并行填充优化
- 上下文压缩

---

## 三、开发优先级

### P0 - 必须完成（核心功能）
1. ✅ 项目结构整理
2. ✅ LLM 客户端封装
3. ✅ Prompt 构建器（四种模式）
4. ✅ 编排调度器（Phase 1-3）
5. ✅ 命令执行器
6. ✅ 编排进度面板
7. ✅ 与 create.vue 基础集成

### P1 - 重要功能（用户体验）
1. LLM 配置面板
2. AI 对话面板
3. 校验报告面板
4. 表格异常高亮
5. 响应解析错误处理

### P2 - 增强功能（可选）
1. 流式响应支持
2. LLM 响应缓存
3. 并行填充优化
4. 多轮对话支持

---

## 四、里程碑

### 里程碑 1：基础设施（第2天）
- [ ] 项目结构整理完成
- [ ] LLM 客户端封装完成
- [ ] 配置管理实现
- [ ] 连接测试通过

### 里程碑 2：Prompt 工程（第4天）
- [ ] 四种 Prompt 模式实现
- [ ] 上下文构建器完成
- [ ] Token 预算管理

### 里程碑 3：编排核心（第7天）
- [ ] 编排调度器实现
- [ ] Phase 1-3 流程跑通
- [ ] 进度管理完成

### 里程碑 4：UI 组件（第10天）
- [ ] 所有 UI 组件完成
- [ ] 与 create.vue 集成
- [ ] 表格高亮实现

### 里程碑 5：测试交付（第13天）
- [ ] 端到端测试通过
- [ ] Prompt 调优完成
- [ ] 性能优化完成

---

## 五、风险与应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| Kimi 2.5 API 响应不稳定 | 中 | 中 | 增加重试机制，降级处理 |
| Token 限制导致上下文截断 | 高 | 中 | 上下文压缩，分页加载 |
| 编排质量不达标 | 中 | 高 | 增加人工确认环节，迭代优化 Prompt |
| 与现有代码集成困难 | 低 | 中 | 提前分析 create.vue 代码结构 |

---

## 六、立即开始的任务

如果你准备开始开发，建议按以下顺序进行：

### 今天可以开始的任务：

1. **项目结构整理**（30分钟）
   - 创建 `src/views/broadcast-plan/` 目录
   - 移动 `create.vue` 到正确位置
   - 更新路由配置

2. **创建核心目录**（15分钟）
   - 创建 `src/services/llm/`
   - 创建 `src/components/llm/`
   - 创建 `src/components/orchestration/`
   - 创建 `src/components/dialogue/`
   - 创建 `src/composables/`
   - 创建 `src/types/`

3. **环境变量配置**（10分钟）
   - 创建 `.env.development` 文件
   - 配置 Kimi API 参数

4. **类型定义**（1小时）
   - 创建 `src/types/llm.ts`
   - 定义所有核心类型

---

*计划版本: v1.0*  
*最后更新: 2026-03-24*
